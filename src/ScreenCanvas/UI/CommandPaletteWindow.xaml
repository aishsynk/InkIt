<Window x:Class="ScreenCanvas.UI.CommandPaletteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InkIt Command Palette"
        Width="560" Height="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize">
  <Window.Resources>
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
  </Window.Resources>
  <Border Margin="16" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10">
    <Border.Effect>
      <DropShadowEffect BlurRadius="24" Direction="270" ShadowDepth="4" Opacity="0.16" Color="#000000"/>
    </Border.Effect>
    <Grid Margin="12">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="1"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- Search Box Row -->
      <Grid Grid.Row="0" Margin="4,2,4,8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="28"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Path Grid.Column="0" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center"
              Fill="{DynamicResource SecondaryTextBrush}" Data="{StaticResource Fluent.Search.Regular}"/>

        <TextBox x:Name="SearchBox" Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center"
                 FontSize="15" BorderThickness="0" Background="Transparent"
                 Foreground="{DynamicResource PrimaryTextBrush}"
                 CaretBrush="{DynamicResource PrimaryTextBrush}"
                 PreviewKeyDown="SearchBox_OnPreviewKeyDown"
                 TextChanged="SearchBox_OnTextChanged"/>

        <Border Grid.Column="2" Background="{DynamicResource HoverBrush}" CornerRadius="4" Padding="6,2">
          <TextBlock Text="ESC to close" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"/>
        </Border>
      </Grid>

      <!-- Separator -->
      <Border Grid.Row="1" Height="1" Background="{DynamicResource SeparatorBrush}" Margin="0,0,0,4"/>

      <!-- Results List -->
      <ListBox x:Name="ResultsList" Grid.Row="2" BorderThickness="0" Background="Transparent"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               ScrollViewer.VerticalScrollBarVisibility="Auto"
               PreviewKeyDown="ResultsList_OnPreviewKeyDown"
               MouseDoubleClick="ResultsList_OnMouseDoubleClick">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Border Padding="8,6" CornerRadius="6" Background="Transparent">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="28"/>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Command Icon -->
                <Path Grid.Column="0" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center"
                      Fill="{DynamicResource PrimaryTextBrush}"
                      Data="{Binding IconGeometry}"/>

                <!-- Name & Description -->
                <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center">
                  <TextBlock Text="{Binding Name}" FontWeight="Medium" FontSize="13" Foreground="{DynamicResource PrimaryTextBrush}"/>
                  <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"
                             TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <!-- Category Badge -->
                <Border Grid.Column="2" Background="{DynamicResource HoverBrush}" CornerRadius="4" Padding="6,2" Margin="8,0" VerticalAlignment="Center">
                  <TextBlock Text="{Binding Category}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                </Border>

                <!-- Shortcut Badge -->
                <Border Grid.Column="3" Background="{DynamicResource HoverBrush}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center"
                        Visibility="{Binding HasShortcut, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <TextBlock Text="{Binding Shortcut}" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}"/>
                </Border>
              </Grid>
            </Border>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      <!-- Footer Info -->
      <Border Grid.Row="3" Margin="4,6,4,0">
        <TextBlock x:Name="StatusText" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"
                   Text="↑↓ to navigate • Enter to select • Esc to dismiss"/>
      </Border>
    </Grid>
  </Border>
</Window>
